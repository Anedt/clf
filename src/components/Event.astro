---
console.log("Event");
//const response = await fetch("...");
//const data = await response.json();
const { Tittel, Av, Lokation, img } = Astro.props;
---

<article>
  <p>{img}</p>
  <h2>{Tittel}</h2>
  <p>Av: {Av}</p>
  <p>Hvor: {Lokation}</p>
</article>

<style>
  .grid {
    display: grid;
    grid-template-columns: ( 1fr 1fr 1fr);
  }

  h1 {
    color: #fff8d2;
    -webkit-text-stroke: 0.1rem;
    -webkit-text-fill-color: transparent;
    font-family: RobR;
    line-height: 2;
  }

  h2 {
    color: #fff8d2;
    font-family: RobR;
    line-height: 2;
  }

  p  {
    color: #b2b8eb;
    font-family: RobR;
    line-
    height: 2;
  }
</style>
